---
name: project_bigmo_rim_execution
description: "RIM Phases 0–2 executed on dreamlab 2026-09-08 (HANDOFF_BIGMO_RIM_v0.2); what is installed, where the site config lives, what is still open (passphrase upload, Gmail IMAP auth, scoot-pmp daemon behind repo)"
metadata: 
  node_type: memory
  type: project
  originSessionId: d3da1ab4-bf52-4052-9304-02f85a6a11af
  modified: 2026-09-08T16:16:49.554Z
---

On 2026-09-08 this session executed `~/HANDOFF_BIGMO_RIM_v0.2.md` Phases 0–2 (plan `~/PLAN_BIGMO_RIM_v0.1.md`, handback `~/HANDBACK_BIGMO_RIM_v0.1.md`). Now installed on dreamlab:

- **Site config (not in any repo):** `~/.scoot-rim/ownership.toml` (positional map, 26 nodes; `$SCOOT_RIM_OWNERSHIP` exported in `.bashrc`), `~/.scoot-rim/events.jsonl` (append-only log), `~/.scoot-rim/check-hook.sh` (warn-only PreToolUse hook on Edit|Write|MultiEdit, wired in `~/.claude/settings.json`; logs to `~/.scoot-rim/check.log` and `gate_warn` events; never blocks).
- **Tools:** `~/scoot-rim-agentd` (generic engine, do not fork), `~/scoot-win-term` (fast-forwarded; `sn`/`sls` sourced from `linux-agent/shell-integration.bash`; `linux-agent/scoot-rim register|adopt|doctor`).
- **systemd:** `scoot-rim-reconcile.timer` enabled (5 min); `scoot-rim-restore.service` installed but disabled on purpose.
- **SIM pages:** `/var/www/html/rim-sim/` on BOTH vhosts (`fairchildlabs.org` + `thedreamlaboratory.org`, the latter needs Apache `ProxyPass /rim-sim !` before the SPA proxy), pattern doc `RIM_architecture_v0.8.md` beside them, linked from the app nav for LEADER|BETA|ENGINEER. **Bundle v0.2 deployed 2026-09-15** (`~/rim-sim-bigmo_v0.2.tar.gz`, committed at `docs/rim-sim/`): level 1 = RIM⁵ with 5 original RI figures, level 2 = **BigMo the machine** (Brandon 2026-09-15: "BigMo is a RIM inside of dreamlab" — dreamlab is the host, not a level), level 3 = **Scoot(34)** with implementations beneath (a later Scoot is a sibling page, not a rewrite). Verify a redeploy by **content type, not status code** — an SPA fallback returns 200 text/html for a missing stylesheet and every page renders unstyled with no error. Never symlink `_shared/`.
- **Memory limits:** app 1 GiB, memory-vault 512 MiB ×2, searxng 512 MiB. Load test 2026-09-08: peak used 2.1 GB, 0 OOM — do not resize the VM.

**RIM compliance, 2026-09-15.** Rescored against `RIM_architecture_v0.8.md` (served at `/rim-sim/`) rather than the questionnaire-era notes, because v0.8 **narrowed three criteria in response to our own answers** — §3.1 now says one-artifact is "not a virtue to buy early" at one author, §3.4 says v0.2's lock disqualifier "was simply wrong" and a carrier rate limit is fine, §3.3 gained "a participant's absence must be survivable AND logged". Three concrete gaps closed that day: (1) `sms_state.pending` was one upserted slot so a second deferred flow destroyed the first silently — migration 0024 `sms_pending_events` is now the truth behind it, answering "deferred and never resumed"; (2) `lib/dependency-health.ts` + `/api/health` `degraded` list, so an optional dependency's absence is logged not invisible; (3) session registry completed. Verdicts deliberately left at partial — **rescoring your own machine upward needs Steve's outside ruling**, per §3.0's own trap.

**Why:** the handoff's rule is "the Map and the log come before terminals"; Phases 3–5 (terminals, enforcement gate, remote view) are deliberately not built.

**How to apply:** start feature sessions *inside* the subtree they work on (a `~` session is denied on strict/handoff nodes — see `scoot/CLAUDE.md` "Ownership and Concurrency"). Pass absolute paths to `scoot-rim-agentd` (relative ones resolve against its own cwd). Shell writes bypass the hook — it only sees Edit/Write. Off-host encrypted snapshot completed 2026-09-09 (`azarchive:archive/dreamlab-snapshots/2026-09-08/`, AES256 symmetric; only Brandon has the passphrase — the pattern for future dumps is: he writes `~/.backup-pass` from a plain terminal, I encrypt/upload/shred). Gmail IMAP fixed 2026-09-11: cause was a Google **password change on fonde.brotherhood@gmail.com at 2026-09-08 12:19 UTC** (Google's security log), which revokes all app passwords + OAuth grants; Brandon issued a new app password, handed over via a 600 file, verified with imaplib before rotating `.env`, app recreated, poller green. Pattern for any Google credential: test it from the host with `imaplib` BEFORE touching `.env`. Google OAuth re-auth done 2026-09-11 for all three connectors (procedure + patches in scoot memory `infra_bigmo_google_mcp.md`); Gmail MCP needs a reconnect to drop its cached token. Still open: publish the GCP consent screen (Testing mode = 7-day expiry); the mail client's stored Fonde Gmail password (mail_accounts id 2) also needs updating in the app; `scoot-pmp` rebuilt+restarted 2026-09-09 at HEAD (works without an Exa key); the other screen session is UNBOUND until `scoot-rim register` runs inside it. Related: [[project_bigmo_rim_questionnaire]].
