# Scoot(34) — The Dream Laboratory (Fonde Brotherhood)

The position for work that belongs to **this Scoot only**: its identity, brand, charter,
label map, and anything a second Scoot would not share. Start sessions here; `sls` shows
`scoot34`.

Cross-Scoot implementations that any Scoot may use keep their own positions:
cards `tools/player-cards`, staking `ri/src/server/trust`, currency `ri/src/server/scoot`.
A second Scoot gets a sibling directory (`scoots/scootNN`), not a change to this one.

| Path | What |
|---|---|
| `brand/name-variants/` | Legal-name wordmark lockups, black and white, plus the proof sheet (2026-09-16) |

`brand/name-variants/build_name_variants.py` was written in another environment: it
imports `build_wordmark` (not included) and writes to `/home/claude/out`. It records how
the PNGs were made; it will not run here as-is.
